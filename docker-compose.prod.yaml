services: 
  backend:
    build: ./backend
    env_file:
      - ./backend/.env
    expose:
      - "4000"
    depends_on:
      - db
      - redis
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.backend.rule=PathPrefix(`/graphql`)"
      - "traefik.http.routers.backend.priority=10"
      - "traefik.http.routers.backend.middlewares=cors-headers@docker,ratelimit@docker"
      - "traefik.http.services.backend.loadbalancer.server.port=4000"
      - "traefik.http.services.backend.loadbalancer.healthcheck.path=/health"
      - "traefik.http.middlewares.cors-headers.headers.accesscontrolalloworiginlist=https://your-vercel-app.vercel.app"
      - "traefik.http.middlewares.cors-headers.headers.accesscontrolallowmethods=GET,POST,OPTIONS"
      - "traefik.http.middlewares.cors-headers.headers.accesscontrolallowheaders=Content-Type,Authorization"
      - "traefik.http.middlewares.cors-headers.headers.accesscontrolallowcredentials=true"
      - "traefik.http.middlewares.ratelimit.ratelimit.average=100"
      - "traefik.http.middlewares.ratelimit.ratelimit.burst=50"
      - "traefik.http.middlewares.ratelimit.ratelimit.period=1s"
    restart: always
    deploy:
      replicas: 1

  db:
    image: postgres:17
    environment:
      POSTGRES_USER: produser
      POSTGRES_PASSWORD: Pr0d_S3cur3_P@ssw0rd_2026!
      POSTGRES_DB: CoreDB
    volumes:
      - db_data:/var/lib/postgresql/data
    restart: always

  redis:
    image: redis:7
    volumes:
      - redis_data:/data
    command: ["redis-server", "--appendonly", "yes"]
    restart: always

  traefik:
    image: traefik:v3.3
    command:
      - "--providers.docker=true"
      - "--entrypoints.web.address=:80"
    ports:  
      - "80:80"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    restart: always

volumes:
  db_data:
  redis_data: